import { ProjectsComments } from './../../entities/ProjectsComments';
import { ResourceWithOptions } from 'adminjs';

export const ProjectsCommentsResource: ResourceWithOptions = {
  resource: ProjectsComments,
  options: {},
};
