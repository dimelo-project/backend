import { Instructors } from './../../entities/Instructors';
import { ResourceWithOptions } from 'adminjs';

export const InstructorsResource: ResourceWithOptions = {
  resource: Instructors,
  options: {},
};
