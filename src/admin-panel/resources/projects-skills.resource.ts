import { ProjectsSkills } from './../../entities/ProjectsSkills';
import { ResourceWithOptions } from 'adminjs';

export const ProjectsSkillsResource: ResourceWithOptions = {
  resource: ProjectsSkills,
  options: {},
};
