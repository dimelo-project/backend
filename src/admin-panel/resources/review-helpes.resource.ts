import { ReviewHelpes } from './../../entities/ReviewHelpes';
import { ResourceWithOptions } from 'adminjs';

export const ReviewHelpesResource: ResourceWithOptions = {
  resource: ReviewHelpes,
  options: {},
};
