import { ResourceWithOptions } from 'adminjs';
import { StudiesComments } from '../../entities/StudiesComments';

export const StudiesCommentsResource: ResourceWithOptions = {
  resource: StudiesComments,
  options: {},
};
