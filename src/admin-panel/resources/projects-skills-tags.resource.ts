import { ProjectsSkillsTags } from './../../entities/ProjectsSkillsTags';
import { ResourceWithOptions } from 'adminjs';

export const ProjectsSkillsTagsResource: ResourceWithOptions = {
  resource: ProjectsSkillsTags,
  options: {},
};
